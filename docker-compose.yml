services:
  ewelink-proxy:
    build: .
    ports:
      - "8001:8001"
    volumes:
      - ./config.yaml:/app/config.yaml
      - proxy-data:/data
    environment:
      - LOG_LEVEL=INFO
    restart: unless-stopped

volumes:
  proxy-data:
